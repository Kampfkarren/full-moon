local name = if local player = getPlayer() then player.Name else "Unknown"
local other = if const player = getPlayer() then player.Name else "Unknown"
local label = if local admin = getAdmin() then admin.title elseif const guest = getGuest() then guest.name else "unknown"
local typed = if local x: number = getValue() then x else 0
